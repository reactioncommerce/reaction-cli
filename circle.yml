machine:
  node:
    version: 7.4.0

test:
  override:
    - echo "No tests!"

deployment:
  npm:
    branch: master
    commands:
      - echo -e "$NPM_USER\n$NPM_PASS\n$NPM_EMAIL" | npm login
      - npm run publish
